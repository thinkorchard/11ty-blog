<li>
  <article class="snippet">
    <img src="{{ post.data.image }}" alt="{{ post.data.imagealt }}" class="snippet__image" />
    <h3 class="snippet__title">
      <a href="{{ post.url }}">{{ post.data.title }}</a>
    </h3>
    <p class="snippet__meta">
      by <span>{{ post.data.author }} on {{ post.date | postDate }}</span>
    </p>
    <p class="snippet__body">
      by <span>{{ post.data.description }}</span>
    </p>
    <a href="{{ post.url }}" class="btn btn--primary">Continue Reading</a>
  </article>
</li>
